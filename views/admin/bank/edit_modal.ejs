  <!-- Modal -->
  <div class="modal fade" id="edit-modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Update Bank</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <form action="/admin/bank?_method=PUT" method = 'POST' enctype="multipart/form-data">  <!-- cara untuk mengirimkan data dari form ke routes, action: routesnya, method: method yang dipakai routes tersebut || enctype="multipart/form-data = untuk mengupload file melalui ejs-->
            <div class="modal-body">
                <div class="form-group">
                  <label for="name">Name Bank</label> <!-- for: sepertinya nda wajib -->
                  <input type="text" class="form-control nameBank" name="nameBank" placeholder="Enter name bank" required> <!-- id: id input, name: data yang akan dikirim ke routes -->
                </div>
                <div class="form-group">
                  <label for="name">Nomor Rekening</label> <!-- for: sepertinya nda wajib -->
                  <input type="text" class="form-control nomorRekening" name="nomorRekening" placeholder="Enter nomor rekening" required> <!-- id: id input, name: data yang akan dikirim ke routes -->
                </div>
                <div class="form-group">
                  <label for="name">Name</label> <!-- for: sepertinya nda wajib -->
                  <input type="text" class="form-control name" name="name" placeholder="Enter name" required> <!-- id: id input, name: data yang akan dikirim ke routes -->
                </div>
                <div class="form-group">
                  <label for="name">Image</label> <!-- for: sepertinya nda wajib -->
                  <input type="file" class="form-control" name="image" > <!-- id: id input, name: data yang akan dikirim ke routes -->
                  <span>Pilih gambar untuk update image</span>
                </div>
            </div>
            <div class="modal-footer">
                <input type="hidden" class="id" name="id">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="submit" class="btn btn-primary">Add</button>
            </div>
        </form>
      </div>
    </div>
  </div>