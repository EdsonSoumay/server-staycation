  <!-- Modal -->
  <div class="modal fade" id="add-modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Add Bank</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <form action="/admin/bank" method = 'POST' enctype="multipart/form-data">  <!-- cara untuk mengirimkan data dari form ke routes, action: routesnya, method: method yang dipakai routes tersebut || enctype="multipart/form-data = untuk mengupload file melalui ejs-->
            <div class="modal-body">
                <div class="form-group">
                  <label for="name">Name Bank</label> <!-- for: sepertinya nda wajib -->
                  <input type="text" class="form-control" name="nameBank" placeholder="Enter name bank" required> <!-- id: id input, name: initialization data yang akan dikirim ke routes -->
                </div>
                <div class="form-group">
                  <label for="name">Nomor Rekening</label> <!-- for: sepertinya nda wajib -->
                  <input type="text" class="form-control" name="nomorRekening" placeholder="Enter nomor rekening" required> <!-- id: id input, name: data yang akan dikirim ke routes -->
                </div>
                <div class="form-group">
                  <label for="name">Name</label> <!-- for: sepertinya nda wajib -->
                  <input type="text" class="form-control" name="name" placeholder="Enter name" required> <!-- id: id input, name: data yang akan dikirim ke routes -->
                </div>
                <div class="form-group">
                  <label for="name">Image</label> <!-- for: sepertinya nda wajib -->
                  <input type="file" class="form-control" name="image" required> <!-- id: id input, name: data yang akan dikirim ke routes -->
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="submit" class="btn btn-primary">Add</button>
            </div>
        </form>
      </div>
    </div>
  </div>